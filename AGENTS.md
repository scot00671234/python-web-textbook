# Agent notes (this repo)

The front end is **Vite + React + TypeScript + React Router** at the **repo root**, not Next.js. Prefer the existing patterns in `src/` and run `npm run build` and `npm run lint` from the repo root when you change code.
