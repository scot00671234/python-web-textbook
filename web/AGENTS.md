# Agent notes (this repo)

The front end is **Vite + React + TypeScript + React Router** under `web/`, not Next.js. Prefer the existing patterns in `web/src/` and run `npm run build` and `npm run lint` from `web/` when you change code.
